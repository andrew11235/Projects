package BoardTiles.StartTile;

import info.gridworld.actor.Actor;

import java.awt.*;

public class StartTile extends Actor {
    public StartTile() {
        this.setColor(new Color(234, 153, 153));
    }
}
